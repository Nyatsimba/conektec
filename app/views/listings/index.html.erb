<div class="jumbotron">
  <h1>La manera más rápida de vender tus productos.</h1>
  <h2>¡Ahora en el Tec CSF! Plataforma diseñada para el Reto Emprendedor</h2>
</div>

<div class="center">
  <div class="row">
    <% @listings.each do |listing| %>
      <div class="col-md-3">
        <div class="thumbnail">
          <%= link_to image_tag(listing.image.url), listing %>
          <div class="caption">
            <h3><%= listing.name %></h3>
            <p><%= number_to_currency(listing.price) %></p>
            <p><%= "Publicado por #{listing.user.name}" %></p>
          </div>
        </div>
      </div>
   <% end %>
  </div>
</div>

<br>

<% if user_signed_in? %>
  <%= link_to 'New Listing', new_listing_path, class: "btn btn-link" %>
<% end %>
<!-- <footer>
  <p style="font-size:.7em;text-align:center">No Image by Ashwini Sukhdeve from The Noun Project</p>
</footer> -->